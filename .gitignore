# Rust build cache
/target

# Intermediate objcopy output (regenerated every build)
/rmk-central.hex
/rmk-peripheral.hex

# Built firmware — never committed. UF2s at repo root and the timestamped
# firmware/<datetime>/ snapshots are local build outputs; regenerate with `just build`.
/rmk-central.uf2
/rmk-peripheral.uf2
/firmware/

# Python bytecode (keymap docs generator)
scripts/__pycache__/

# macOS
.DS_Store

# Generated keymap documentation (regenerate with: just docs)
/*.svg
/*.html
